<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.amusing.start.user.mapper.plus.UserAccountInfoMapperPlus">

  <update id="updateMainAccount">
    update user_account_info set main_amount = main_amount - #{updateAmount} where user_id = #{userId} and main_amount = #{originalAmount}
  </update>

  <update id="updateGiveAccount">
    update user_account_info set give_amount = give_amount - #{updateAmount} where user_id = #{userId} and give_amount = #{originalAmount}
  </update>

</mapper>